name: Deploy website to Pages

on:
    push:
    workflow_dispatch:
    schedule:
        # Runs everyday at 8:00 AM
        - cron: "0 0 * * *"

# Sets permissions of the GITHUB_TOKEN to allow deployment to GitHub Pages
permissions:
  contents: read
  pages: write
  id-token: write

jobs:
    sync-submodule:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v2
            - name: Pull & update submodules recursively
              run: |
                cd content/
                git pull
                cd ..
                git submodule update --init --remote --recursive content
            - name: Commit & push changes
              run: |
                #git config --global user.name "spike"
                #git config --global user.email "2899561390@qq.com"
                git commit -am "[auto] Update submodules content" || echo "No changes to commit"
            - name: Push Changes
              uses: ad-m/github-push-action@master
              with: 
                github_token: ${{ secrets.GITHUB_TOKEN }}
    build:
        runs-on: ubuntu-latest
        needs: sync-submodule
        steps:
            - name: Checkout
              uses: actions/checkout@v2
              with:
                  submodules: true
                  fetch-depth: 0

            - name: Setup Hugo
              uses: peaceiris/actions-hugo@v2
              with:
                  hugo-version: "latest"
                  extended: true

            - name: Build Web
              run: hugo

            - name: Upload artifact
              uses: actions/upload-pages-artifact@v1
              with:
                path: ./public

    deploy:
      environment:
        name: github-pages
        url: ${{ steps.deployment.outputs.page_url }}
      runs-on: ubuntu-latest
      needs: build
      steps:
        - name: Deploy to GitHub Pages
          id: deployment
          uses: actions/deploy-pages@v1
